const initState = {

}


export const LoginReducer = (state = initState, action: any): any => { // fix any
    switch (action.type) {
        case '': {
            return state
        }

        default: return state
    }
}
